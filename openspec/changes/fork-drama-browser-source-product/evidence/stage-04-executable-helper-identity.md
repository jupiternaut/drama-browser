# Stage 04 - Executable and Helper Product Identity

Status: passed

Date: 2026-06-29

## Source Workspace

- Source workspace: `/Users/gengrf/zen-browser-desktop`
- Wrapper/OpenSpec workspace: `/Users/gengrf/drama-browser`
- Current source-built app: `/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser/Drama Browser.app`
- Current DMG: `/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser-1.21.2b.en-US.mac.dmg`

## Build and Package

Command:

```bash
PATH="$HOME/.cargo/bin:$PATH" \
SURFER_PLATFORM=darwin \
SURFER_COMPAT=aarch64 \
ZEN_RELEASE=1 \
ZEN_GA_DISABLE_PGO=true \
npm run package
```

Result: passed.

Packaging produced:

```text
/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser/Drama Browser.app
/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser-1.21.2b.en-US.mac.dmg
/Users/gengrf/zen-browser-desktop/dist/drama-browser-1.21.2b.en-US.mac.dmg
/Users/gengrf/zen-browser-desktop/dist/output.mar
```

## Main App Identity

`Info.plist`:

```text
CFBundleName: Drama Browser
CFBundleIdentifier: app.drama-browser.local
CFBundleExecutable: drama-browser
CFBundleURLSchemes: http, https, drama, file
```

This proves the main product identity is generated by the source build and is not a post-package rename.

## Helper Identity

Helper app `Info.plist` values:

```text
drama-browser-gpu-helper.app
CFBundleIdentifier: app.drama-browser.drama-browser-gpu-helper
CFBundleExecutable: Drama Browser GPU Helper

drama-browser-media-plugin-helper.app
CFBundleIdentifier: app.drama-browser.drama-browser-media-plugin-helper
CFBundleExecutable: Drama Browser Media Plugin Helper

drama-browser-plugin-container.app
CFBundleIdentifier: app.drama-browser.drama-browser-plugin-container
CFBundleExecutable: drama-browser-plugin-container
```

The inherited updater remains:

```text
updater.app
CFBundleIdentifier: org.mozilla.updater
CFBundleExecutable: org.mozilla.updater
```

This is not a Zen helper identity failure. It is the explicit Stage 05 target.

## Product String Scan

Text scan over the source-built app bundle for product identity strings:

```bash
rg -n --hidden \
  --glob '*.plist' --glob '*.ini' --glob '*.js' \
  --glob '*.json' --glob '*.properties' --glob '*.strings' \
  'Zen Browser|app\.zen-browser|zen-browser|zenhelper|Zen GPU|Zen Helper' \
  "/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser/Drama Browser.app/Contents"
```

Result: no matches.

Path scan for `Zen`/`zen` produced only normal cursor filenames such as `sizeNW.png`; no Zen product path was found in app identity locations.

## Runtime Launch Check

Command launched the source-built executable with a temporary profile:

```bash
"/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser/Drama Browser.app/Contents/MacOS/drama-browser" \
  --no-remote \
  --profile /tmp/drama-stage04-profile.WgcKQ6 \
  about:blank
```

Observed process/log identity:

```text
/Users/gengrf/zen-browser-desktop/engine/obj-aarch64-apple-darwin/dist/drama-browser/Drama Browser.app/Contents/MacOS/drama-browser
Drama Browser GPU Helper
```

No source-built process identified itself as `Zen Browser`, `zen-browser`, or `Zen GPU Helper`.

There were unrelated already-running wrapper processes from `/Users/gengrf/drama-browser/dist/zen-drama-mac/Drama Browser.app`. Those were excluded from this stage because they are not descendants of the source-built app process.

## Verifier

Source identity verifier:

```bash
node scripts/verify-drama-source-identity.ts \
  --source-workspace /Users/gengrf/zen-browser-desktop \
  --out openspec/changes/fork-drama-browser-source-product/evidence/stage-04-source-identity.json
```

Result: `ok: true`

Passed checks:

- `surfer.json` resolves `binaryName=drama-browser`
- generated `engine/mozconfig` resolves `--with-app-name=drama-browser`
- macOS bundle id suffix is source-owned by Drama branding config
- plugin, media, GPU, and security module helper seeds are Drama-owned
- hardened runtime entitlement no longer points at `app.zen-browser.zen`
- Stage 4 product identity source fields do not reference Zen

## Boundary to Stage 05

The source-built artifact still packages Mozilla updater components:

```text
Contents/MacOS/updater.app/Contents/MacOS/org.mozilla.updater
Contents/Library/LaunchServices/org.mozilla.updater
Contents/Resources/updater.ini
```

Updater ownership is the next acceptance gate.
